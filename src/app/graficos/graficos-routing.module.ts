import { NgModule } from '@angular/core';
import { RouterModule, Routes } from '@angular/router';
import { Graficos2Component } from './grafico2.component';

export const routes: Routes = [
    {
        path: '',
        component: Graficos2Component,
    },
];
